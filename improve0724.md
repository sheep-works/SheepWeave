# 2026年7月24日 のアップデートまとめ (SheepWeave)

本日実施した細かな修正・新機能追加などの成果をまとめました。

## 1. エディタ保護とファイル追加機能の強化
- **エディタ自動保存＆クローズ機能の追加**: 
  意図せぬUndoエラーや行数ガード発動を防ぐため、データ読み込み時やファイル追加時に、開かれている `.shwvt` / `.shwvs` ファイルを自動的に保存し閉じるセーフガード（`saveAndCloseShwvEditors`）を実装しました。
- **インクリメンタルなソース追加（Add Files）**: 
  `Working/02_SOURCE` に後から追加されたファイルのみを抽出し、既存の翻訳データを維持したままプロジェクトの末尾に安全にインデックスを追加する機能（`incrementalAddSource`）を実装しました。

## 2. Webview (Flowタブ) のUI最適化と整理
- **アコーディオン（Collapse）レイアウトの導入**: 
  `FlowTab.vue` のUIをタイムラインからアコーディオン形式に変更し、画面をすっきりさせました。「初期化」「準備」「作業中」「完了」の4つのセクションに整理し、デフォルトで「作業中」が開くように設定しました。
- **言語設定の自由度向上**: 
  言語選択プルダウンに `allow-create` と `allow-search` を追加し、ユーザーが標準以外の任意の言語コード（例: `ja`, `en` 等）を直接入力・作成できるようにしました。

## 3. ファイル操作・詳細設定へのアクセス改善
- **ディレクトリ表示の改善**: 
  「現在のディレクトリを開く」コマンドを実行した際、親フォルダ内で選択される形から、**対象のルートディレクトリそのものがエクスプローラーで直接開かれる**ように挙動を変更しました。
- **詳細設定（workflow.ini）へのショートカット**: 
  Flowタブの初期化セクションに「詳細設定を開く」ボタンを追加し、クリックするだけでVS Code上で `workflow.ini` を直接編集できるようにしました。

## 4. コンコーダンス検索（Concordance）のシームレス化
- **検索キーワードの自動入力・同期**: 
  エディタ側で `Ctrl+K` を使用してコンコーダンス検索を実行した際、検索結果と共に、**Webview側の入力ウインドウ（キーワードとSource/Targetの選択）にも自動的に内容が同期・反映**されるようにしました。
- **Webview内でのキーボードショートカット対応**: 
  Webview全体（Translateタブ等）のショートカット処理を追加し、**テキストを選択して `Ctrl+K` (Source) または `Ctrl+Shift+K` (Target) を押すだけで、直接コンコーダンス検索が実行**できるようにしました。
